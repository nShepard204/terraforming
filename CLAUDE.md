# Never push without my explicit permission.

# The frontend dev server is always running on port 5173. Reuse it.

# The backend dev server is always running on port 3000. Reuse it.

# Before you make any changes to the codebase, always create a new git branch (local and remote) based off of the 'preview' branch and make your changes on that branch.
